name: plannight
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Ships Material/Cupertino widget translations (date pickers, "Cancel", ...)
  # for the locales we support. See lib/core/l10n_delegates.dart for how Tajik —
  # which Flutter has no built-in translations for — is handled.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  dio: ^5.10.0
  flutter_secure_storage: ^10.3.1
  # Pinned to a matched drift/drift_dev pair: drift 2.34.1 + drift_dev 2.34.0 is
  # a broken combination (drift_dev calls sqlparser APIs newer than its own
  # constraint allows). 2.33.0 for both resolves codegen cleanly.
  drift: 2.33.0
  drift_flutter: ^0.3.0
  sqlite3_flutter_libs: ^0.6.0+eol
  path_provider: ^2.1.6
  path: ^1.9.1
  fl_chart: ^1.2.0
  # Pinned by flutter_localizations, which depends on an exact intl version.
  # Anything newer makes the whole solve fail — let the SDK pick it.
  intl: any
  connectivity_plus: ^7.2.0
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Generates the launcher icon (all mipmap densities + Android adaptive icon)
  # from the brand mark in assets/icon/. Run: `dart run flutter_launcher_icons`.
  flutter_launcher_icons: ^0.14.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  drift_dev: 2.33.0

# Workaround: sqlparser 0.44.6 removed a `DartPlaceholder.when` helper that
# drift_dev 2.33/2.34 still call, breaking codegen. Pin sqlparser to an earlier
# 0.44.x that still has it. (sqlparser is only used by drift_dev at build time.)
dependency_overrides:
  sqlparser: 0.44.0

# Launcher-icon generation (dart run flutter_launcher_icons). The brand mark:
# a light crescent moon + check on a deep-navy tile. The adaptive icon uses the
# navy as its background and the transparent moon+check as the foreground, so it
# looks right under any device mask (circle, squircle, rounded square).
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon_full.png"
  adaptive_icon_background: "#1E2A56"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Runs `gen-l10n` on every build: turns lib/l10n/*.arb into the generated
  # AppLocalizations class. Configured by l10n.yaml at the package root.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Brand typography (bundled, so it works offline). Both are variable fonts —
  # a single file per family serves every weight, and Flutter maps `fontWeight`
  # onto the wght axis. Plus Jakarta Sans carries the UI; Space Grotesk is the
  # numeric/monospace accent used for times, stats and dates. OFL licensed.
  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-Variable.ttf
    - family: SpaceGrotesk
      fonts:
        - asset: assets/fonts/SpaceGrotesk-Variable.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
